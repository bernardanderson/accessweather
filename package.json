{
  "name": "accessweather",
  "version": "1.0.0",
  "description": "A small application for accepting WeatherUnderground data from an Accurite Access device, storing the data in a SQLite Db and retrieving/displaying that data",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "GPL",
  "dependencies": {
    "better-sqlite3": "^5.0.1",
    "express": "^4.16.4"
  }
}
